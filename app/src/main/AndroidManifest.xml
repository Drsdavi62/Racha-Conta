<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.application.billsplitingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bill"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bill_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".BillSplitApp">
        <activity
            android:name=".ui.presentation.HomeActivity"
            android:exported="true"
            android:theme="@style/splashScreen"
            android:windowSoftInputMode="adjustPan|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".allBills.AllBillsActivity"
            android:theme="@style/splashScreen">

        </activity>
        <activity android:name=".MainActivity" android:theme="@style/AppThemeNotCompose"/>
    </application>

</manifest>